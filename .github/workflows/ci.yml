name: Firebase CI/CD Deploy

on:
  push:
    branches:
      - main  # main ブランチに push したら自動実行

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 履歴をすべて取得

      # 2. Node.js をセットアップ
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json

      # 3. 依存パッケージをインストール
      - name: Install dependencies
        run: npm ci

      # 4. Firebase CLI をインストール
      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      # 5. ビルド & デプロイ
      - name: Build and Deploy to Firebase
        run: |
          npm run buildgit    # ローカルで動作確認済みのビルドコマンド
          firebase deploy --token $FIREBASE_TOKEN
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}  # GitHub Secrets に登録済みのトークン
